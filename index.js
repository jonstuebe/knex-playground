const knex = require("knex")(require("./knexfile"));
const { get, map } = require("lodash");
const Promise = require("bluebird");
const fs = require("fs-extra");
const { log, savePoint, stringify, transaction } = require("./helpers");

(async () => {
  //
})();
